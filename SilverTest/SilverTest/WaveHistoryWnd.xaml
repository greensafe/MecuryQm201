<Window x:Class="SilverTest.WaveHistoryWnd"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:dw="http://research.microsoft.com/DynamicDataDisplay/1.0"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SilverTest"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        Title="历史数据" Height="450" Width="714.056" Loaded="Window_Loaded">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel Width="200" Background="LightGray">
                <ListBox x:Name="filelsb" DisplayMemberPath="Filename" SelectionChanged="filelsb_SelectionChanged">
                    <!--
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button x:Name="itemlistbtn" Click="itemlistbtn_Click">
                                <Grid>
                                    <TextBlock Text="{Binding Path=Filename}" Margin="5 5" FontSize="10"></TextBlock>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    -->
                </ListBox>
            </StackPanel>
            <StackPanel Width="500" Margin="5,0,0,0" Background="LightGray">
                <dw:ChartPlotter x:Name="realCpt" Width="400" Height="200" DockPanel.Dock="Top" Margin="10,10,20,10"></dw:ChartPlotter>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
